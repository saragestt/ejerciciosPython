#Diseñe un programa que lea un número entero (positivo o negativo) y
#determine si tiene 1, 2,3, 4 o más cifras, visualizando lo que corresponda.

introduce = input("Introduce un numero:\n")
cifras = len(introduce)
print(cifras)